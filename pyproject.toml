[project]
name = "mcp-crawl4ai2vectordb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "python-dotenv",
    "mcp[cli]",
    "supabase",
    "crawl4ai",
    "openai",
]
